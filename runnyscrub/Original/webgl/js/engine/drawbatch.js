function drawBatch(texture_name,
					 start_index,
					 num_indices,
					 num_triangles
					) {
		this.tex = texture_name;
		this.start_index = start_index;
		this.num_indices = num_indices;
		this.num_triangles = num_triangles;
	}
	
